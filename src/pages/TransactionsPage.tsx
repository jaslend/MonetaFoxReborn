import { PagePlaceholder } from './PagePlaceholder';

export function TransactionsPage() {
  return (
    <PagePlaceholder
      title="Transactions"
      description="Enter, edit, tag, split, and reconcile transactions."
    />
  );
}
